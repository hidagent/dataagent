# DataAgent Server Demo

基于 Streamlit 的 DataAgent Server 测试界面，供后端开发同学验证 API 功能。

## 功能

- 🔌 支持 WebSocket 流式聊天
- 📡 支持 REST API 同步聊天
- 🔑 支持 API Key 认证
- 📋 会话管理和浏览
- 🔧 工具调用可视化
- ⚠️ HITL 审批展示（自动批准）

## 安装

```bash
cd libs/dataagent-server-demo
pip install -e .
```

## 使用

### 方式一：命令行启动

```bash
streamlit run dataagent_server_demo/app.py
```

### 方式二：使用入口点

```bash
dataagent-demo
```

## 配置

在侧边栏配置：

- **Host**: DataAgent Server 地址（默认 localhost）
- **Port**: 端口号（默认 8000）
- **Use SSL**: 是否使用 HTTPS/WSS
- **API Key**: 认证密钥（可选）

## 测试流程

1. 启动 DataAgent Server：
   ```bash
   cd libs/dataagent-server
   uvicorn dataagent_server.main:app --reload
   ```

2. 启动 Demo：
   ```bash
   cd libs/dataagent-server-demo
   streamlit run dataagent_server_demo/app.py
   ```

3. 在浏览器中打开 http://localhost:8501

4. 点击 "Check Health" 验证连接

5. 输入消息开始测试

## 截图

```
┌─────────────────────────────────────────────────────────┐
│  ⚙️ Configuration          │  🤖 DataAgent Server Demo  │
│  ─────────────────         │  Session: `abc123...`      │
│  Server                    │                            │
│  Host: [localhost    ]     │  ┌────────────────────┐    │
│  Port: [8000         ]     │  │ User: Hello!       │    │
│  [x] Use SSL               │  └────────────────────┘    │
│  API Key: [********  ]     │  ┌────────────────────┐    │
│                            │  │ Assistant: Hi!     │    │
│  [🔍 Check Health]         │  │ How can I help?    │    │
│  ─────────────────         │  └────────────────────┘    │
│  Session                   │                            │
│  [🔄 New] [🗑️ Clear]       │  [Type your message...  ]  │
└─────────────────────────────────────────────────────────┘
```
